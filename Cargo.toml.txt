[package]
name = "rocket_os"
version = "0.1.0"
edition = "2021"

[dependencies]
# Using `rand` for simulating sensor noise in dummy HAL
rand = "0.8"
# Using `chrono` for timestamping simulation
chrono = "0.4"
# Using `lazy_static` for global dummy hardware state (simplifies example)
lazy_static = "1.4"